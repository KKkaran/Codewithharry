import React from 'react'

export default function Header({title,name}) {
return (
    <>
        <div>{title}</div>
        <h3>{name}</h3>dsds
        
        
        {
            //console.log(title)
        }
    </>
    )
}